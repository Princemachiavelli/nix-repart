# Params: copy from (localfs or network, NFS)., nixosConfig to install.
* Use for keeping stateful data on disk.
# Btrfs read-only project or overlayfs:
* In initrd: switch to read-only or read-write.
